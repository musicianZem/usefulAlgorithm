# make [Ctrl + Tab] & [Ctrl + Shift + Tab] in order

## Put in (Preferences -> Key Bindings - User):

```  
{ "keys": ["ctrl+tab"], "command": "next_view" },  
{ "keys": ["ctrl+shift+tab"], "command": "prev_view" }  
```  
